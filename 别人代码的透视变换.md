# YOLOv11+CNN车牌识别系统分析报告

## 1. 工程概述

本工程实现了一个基于YOLOv11和CNN的车牌识别系统，主要功能包括车牌检测、车牌矫正和字符识别。系统能够处理倾斜的车牌图像，通过透视变换将其矫正为标准尺寸，然后使用CNN模型识别车牌字符。

## 2. 透视变换实现

### 2.1 核心实现文件

透视变换功能主要在以下文件中实现：

- **`plate_align.py`** - 核心实现文件
- **`MainUI.py`** - 调用文件

### 2.2 透视变换框架

透视变换基于 **OpenCV** 库实现，核心步骤如下：

1. **角点获取**：通过YOLO检测车牌边界框，生成初始角点
2. **角点优化**：使用轮廓分析优化角点位置
3. **角点排序**：确保四个角点按正确顺序排列
4. **透视变换**：应用OpenCV的透视变换函数将倾斜车牌转换为正矩形

### 2.3 关键代码分析

#### 2.3.1 角点排序函数

```python
def order_points(pts):
    centroid = np.mean(pts, axis=0)
    angles = np.arctan2(pts[:, 1] - centroid[1], pts[:, 0] - centroid[0])
    sorted_indices = np.argsort(angles)
    sorted_pts = pts[sorted_indices]
    top_left_idx = np.argmin(sorted_pts[:, 1])
    ordered_pts = np.roll(sorted_pts, -top_left_idx, axis=0)
    return ordered_pts
```
<file_path>c:\Users\Y9000P\Downloads\2026ICContest\YOLOv11+CNN车牌识别\ultralytics-LPR\plate_align.py:141-149</file_path>

#### 2.3.2 透视变换函数

```python
def four_point_transform(image, pts):
    rect = order_points(pts)
    target_height = 145
    target_width = 450
    warped_img = []
    dst = np.array([
        [0, 0],
        [target_width - 1, 0],
        [target_width - 1, target_height - 1],
        [0, target_height - 1]
    ], dtype="float32")

    M = cv2.getPerspectiveTransform(rect, dst)
    warped = cv2.warpPerspective(image, M, (target_width, target_height))
    warped_img.append(warped)

    return warped, warped_img
```
<file_path>c:\Users\Y9000P\Downloads\2026ICContest\YOLOv11+CNN车牌识别\ultralytics-LPR\plate_align.py:152-168</file_path>

#### 2.3.3 角点优化函数

```python
def refine_corners_with_contours(image, initial_corners, result, weight=0.975):
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
    # 自适应阈值创建二值图像
    binary = cv2.adaptiveThreshold(gray, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
                                   cv2.THRESH_BINARY_INV, 11, 2)

    # 在边界框内查找轮廓
    x_min, y_min = int(initial_corners[0][0]), int(initial_corners[0][1])
    x_max, y_max = int(initial_corners[2][0]), int(initial_corners[2][1])

    # 确保坐标在图像范围内
    x_min = max(0, x_min)
    y_min = max(0, y_min)
    x_max = min(image.shape[1] - 1, x_max)
    y_max = min(image.shape[0] - 1, y_max)

    roi = binary[y_min:y_max, x_min:x_max]
    contours, _ = cv2.findContours(roi, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)

    if not contours:
        print("未在边界框内找到轮廓")
        return initial_corners

    # 找到最大轮廓
    largest_contour = max(contours, key=cv2.contourArea)
    largest_contour = largest_contour + np.array([[x_min, y_min]])

    # 加权距离优化角点
    refined_corners = []
    top_line = (initial_corners[0], initial_corners[1])  # 上边
    right_line = (initial_corners[1], initial_corners[2])  # 右边
    bottom_line = (initial_corners[2], initial_corners[3])  # 下边
    left_line = (initial_corners[3], initial_corners[0])  # 左边

    lines = [top_line, right_line, bottom_line, left_line]

    # 对每个角点进行优化
    for i, corner in enumerate(initial_corners):
        min_distance = float('inf')
        best_point = corner

        line1 = lines[i]
        line2 = lines[(i - 1) % 4]

        # 在轮廓点中寻找最佳匹配点
        for point in largest_contour[:, 0, :]:
            point = point.astype(float)

            # 计算点到两条边的距离
            dist_to_line1 = point_to_line_distance(point, line1[0], line1[1])
            dist_to_line2 = point_to_line_distance(point, line2[0], line2[1])
            dist_to_corner = np.linalg.norm(point - corner)
            weighted_distance = (weight * (dist_to_line1 + dist_to_line2) +
                                 (1 - weight) * dist_to_corner)

            if weighted_distance < min_distance:
                min_distance = weighted_distance
                best_point = point

        refined_corners.append(best_point)

    return np.array(refined_corners, dtype="float32")
```
<file_path>c:\Users\Y9000P\Downloads\2026ICContest\YOLOv11+CNN车牌识别\ultralytics-LPR\plate_align.py:57-118</file_path>

## 3. YOLO到LPRNET的变换流程

### 3.1 完整处理流程

1. **车牌检测阶段（YOLO）**
   - 加载YOLOv11模型
   - 检测输入图像中的车牌
   - 提取边界框坐标
   - 生成初始角点

2. **车牌矫正阶段**
   - 优化角点位置
   - 应用透视变换矫正车牌

3. **字符识别阶段（CNN）**
   - 预处理矫正后的车牌图像
   - 使用CNN模型预测字符
   - 解码识别结果

### 3.2 调用关系

```
MainUI.py
├── img_predict() - 检测车牌并生成初始角点
│   ├── model.predict() - YOLO检测
│   ├── refine_corners_with_contours() - 优化角点
│   └── four_point_transform() - 透视变换
└── carNumber_predict() - 识别车牌字符
    └── cnn_predict() - CNN字符识别
```

## 4. 实时处理分析

### 4.1 性能分析

| 处理阶段 | 时间复杂度 | 实际处理时间 |
|---------|-----------|------------|
| YOLO检测 | O(n) | 几毫秒 |
| 角点优化 | O(n) | 几毫秒 |
| 透视变换 | O(n) | 几毫秒 |
| CNN识别 | O(n) | 几毫秒 |

### 4.2 实时处理可行性

- **优势因素**：
  - YOLOv11推理速度快
  - 透视变换计算效率高
  - CNN模型结构紧凑
  - 各阶段处理时间均在毫秒级

- **优化建议**：
  - 模型量化：提高推理速度
  - 并行处理：提高并发性能
  - 内存优化：减少中间变量
  - 模型压缩：使用更轻量级的模型

## 5. 系统架构图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│ 输入图像    │────>│ YOLO检测    │────>│ 透视变换    │────>│ CNN识别     │────>│ 输出结果    │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                        │                     │                     │
                        ▼                     ▼                     ▼
                ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
                │ 边界框提取  │     │ 角点优化    │     │ 字符解码    │
                └─────────────┘     └─────────────┘     └─────────────┘
```

## 6. 代码优化建议

1. **模型优化**
   - 使用模型量化技术，将模型精度从FP32降低到INT8
   - 考虑使用TensorRT或ONNX Runtime加速推理

2. **算法优化**
   - 角点优化算法可以使用更高效的实现
   - 考虑使用并行计算加速处理

3. **内存管理**
   - 减少中间变量的创建
   - 使用内存池管理重复使用的内存

4. **代码结构**
   - 模块化设计，将不同功能封装为独立模块
   - 增加错误处理和日志记录

## 7. 总结

本工程实现了一个完整的车牌识别系统，通过YOLOv11检测车牌，透视变换矫正车牌，CNN识别字符。系统设计合理，计算复杂度适中，适合实时处理应用场景。

透视变换作为系统的核心环节，通过OpenCV实现了高效的车牌矫正，为后续的字符识别提供了高质量的输入。整个系统的处理流程清晰，代码结构合理，具有良好的可维护性和扩展性。

在实际应用中，可以根据硬件性能和实时性要求进行适当的优化，以达到更好的实时处理效果。